prefix=@prefix@
exec_prefix=@exec_prefix@
libdir=@libdir@
includedir=@includedir@

Name: sphinxclient
Description: Sphinx searchserver C++ client library, an alternative to the native C library distributed with sphinx. The communication module is written using unix sockets, so this library is only suitable for unix-like systems.
Version: @VERSION@
Requires:
Requires.private:
Libs: -L${libdir} -lsphinxclient
Libs.private: -lrt
Cflags: -I${includedir}
